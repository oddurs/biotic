// Generated by scripts/generate.ts. Do not edit; run `pnpm generate`.
import * as stylex from "@stylexjs/stylex";
import { color } from "./color.stylex";
import { palette as p } from "./palette.stylex";

/** Forces the light face regardless of system preference. Apply to <html>. */
export const lightTheme = stylex.createTheme(color, {
  surfaceCanvas: p.glass50,
  surfaceRaised: "oklch(100% 0 0)",
  surfaceSunken: p.glass100,
  surfaceInverse: p.glass950,
  inkPrimary: p.glass950,
  inkSecondary: p.glass700,
  inkMuted: p.glass500,
  inkInverse: p.glass50,
  edgeSubtle: p.glass200,
  edgeStrong: p.glass300,
  edgeFocus: p.agar600,
  accentBase: p.agar600,
  accentHover: p.agar700,
  accentSoft: p.agar100,
  accentInk: "oklch(100% 0 0)",
  positiveBase: p.agar600,
  positiveSoft: p.agar100,
  cautionBase: p.nutrient600,
  cautionSoft: p.nutrient100,
  negativeBase: p.lysis600,
  negativeSoft: p.lysis100,
  infoBase: p.pheromone600,
  infoSoft: p.pheromone100,
  selection: p.agar200,
});

/** Forces the dark face regardless of system preference. Apply to <html>. */
export const darkTheme = stylex.createTheme(color, {
  surfaceCanvas: p.glass950,
  surfaceRaised: p.glass900,
  surfaceSunken: "oklch(15% 0.008 155)",
  surfaceInverse: p.glass50,
  inkPrimary: p.glass50,
  inkSecondary: p.glass300,
  inkMuted: p.glass500,
  inkInverse: p.glass950,
  edgeSubtle: p.glass800,
  edgeStrong: p.glass700,
  edgeFocus: p.agar400,
  accentBase: p.agar400,
  accentHover: p.agar300,
  accentSoft: p.agar900,
  accentInk: p.glass950,
  positiveBase: p.agar400,
  positiveSoft: p.agar900,
  cautionBase: p.nutrient400,
  cautionSoft: p.nutrient900,
  negativeBase: p.lysis400,
  negativeSoft: p.lysis900,
  infoBase: p.pheromone400,
  infoSoft: p.pheromone900,
  selection: p.agar800,
});
