---
title: CLI reference
description: Every biotic command and its arguments.
section: Reference
order: 0
generated: scripts/cli_reference.py
---

Generated from the command line parser by `scripts/cli_reference.py`; edit the code, not this page.
Run any command as `uv run biotic <command>` from the repository, or `biotic <command>` once installed. Without a command, `biotic` opens the live view if a culture exists.

## seed

Inoculate a fresh dish from a word, phrase, or question.

| Argument | Meaning |
| --- | --- |
| `seed` | — |
| `--fresh` | autoclave first if a culture exists |
| `--budget BUDGET` | dollars this dish may spend on the mind (default: what the dish remembers, else BIOTIC_BUDGET_USD, else 2.00; inf for no cap) |

## live

Watch the dish (default).

| Argument | Meaning |
| --- | --- |
| `--tick TICK` | seconds per tick |
| `--budget BUDGET` | dollars this dish may spend on the mind (default: what the dish remembers, else BIOTIC_BUDGET_USD, else 2.00; inf for no cap) |
| `--clock CLOCK` | mutagen clock: wall (a background thread; the dish never waits on the mind; live's default) or tick (the dish calls the mind itself, at most every BIOTIC_MUTAGEN_EVERY_TICKS ticks, and waits for the reply; run's default). Sticks to the dish like --budget; BIOTIC_MUTAGEN_CLOCK sets it per process. docs/experiments.md One of `wall`, `tick` |

## run

Run headless, e.g. for an experiment.

| Argument | Meaning |
| --- | --- |
| `--ticks TICKS` | — |
| `--tick TICK` | seconds per tick (0 = as fast as possible) |
| `--quiet` | no budget line or progress reports; the summary at the end still prints |
| `--budget BUDGET` | dollars this dish may spend on the mind (default: what the dish remembers, else BIOTIC_BUDGET_USD, else 2.00; inf for no cap) |
| `--clock CLOCK` | mutagen clock: wall (a background thread; the dish never waits on the mind; live's default) or tick (the dish calls the mind itself, at most every BIOTIC_MUTAGEN_EVERY_TICKS ticks, and waits for the reply; run's default). Sticks to the dish like --budget; BIOTIC_MUTAGEN_CLOCK sets it per process. docs/experiments.md One of `wall`, `tick` |

## status

## strains

Census of living strains.

| Argument | Meaning |
| --- | --- |
| `--all` | include extinct |

## genome

Print a strain's code (`top` for the dominant one).

| Argument | Meaning |
| --- | --- |
| `id` | — |

## log

| Argument | Meaning |
| --- | --- |
| `-n N` |  (default `30`) |

## curve

Plot the growth curve: population and strains by tick, with phase and drop markers.

| Argument | Meaning |
| --- | --- |
| `--cols COLS` | comma-separated columns of curve.csv; the first is the main panel, the rest are drawn under it (default `population,strains`) |
| `--since TICK` | rows from this tick on |
| `--branch N` | which timeline after a revive (default: the last) |
| `--png OUT` | write a PNG with matplotlib instead of printing (the plot extra) |
| `--width WIDTH` | columns (default: the terminal's) |
| `--height HEIGHT` | rows of the main panel; the others get half (default `12`) |

## notes

The naturalist's field notes.

| Argument | Meaning |
| --- | --- |
| `-n N` | the last N entries (0 for the whole notebook) (default `5`) |

## whisper

Pin a note the mutagen will see.

| Argument | Meaning |
| --- | --- |
| `text...` | — |

## drop

Intervene in the dish.

| Argument | Meaning |
| --- | --- |
| `what` | One of `nutrient`, `antibiotic`, `mutagen` |
| `--at AT` | x,y |
| `--r R` | radius |

## minds

List models available to the mind.

| Argument | Meaning |
| --- | --- |
| `query` | — |

## probe

Check the mind answers.

## sterilize

Autoclave everything (the freezer is kept unless --freezer).

| Argument | Meaning |
| --- | --- |
| `--yes, -y` | — |
| `--freezer` | empty the freezer too |

## freeze

Put the dish, or one strain, in the freezer.

| Argument | Meaning |
| --- | --- |
| `--label LABEL` | a-z, 0-9, _ and -; default `manual` |
| `--strain ID` | freeze one strain's genome and a cell's memory instead |

## revive

Replace the dish with a frozen sample, or inoculate a frozen strain.

| Argument | Meaning |
| --- | --- |
| `key` | a tick, or a sample name from `biotic freezer` |
| `--label LABEL` | with a tick: which of the samples at that tick |
| `--strain ID|SAMPLE` | a strain sample instead of a dish sample |
| `--into INTO` | a fresh dish (default; the incubator must be stopped) or the current one One of `fresh`, `current` |
| `--n N` | cells to inoculate, fresh or current (default 5) |
| `--at AT` | x,y — where in the current dish (default: the centre); --into current only |
| `--yes, -y` | do not ask before replacing the dish |

## freezer

What is in the freezer.

| Argument | Meaning |
| --- | --- |
| `--json` | — |
