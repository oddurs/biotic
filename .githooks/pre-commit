#!/bin/sh
# Formatting and lint must be clean before a commit exists. Both are fast, so the whole tree is checked.
set -eu
root=$(git rev-parse --show-toplevel)
"$root/scripts/task" fmt:check
"$root/scripts/task" lint
